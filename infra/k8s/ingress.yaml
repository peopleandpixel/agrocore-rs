apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agrocore-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: api.agrocore.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: agrocore-api-service
            port:
              number: 80
