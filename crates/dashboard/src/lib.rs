//! AgroCore Live Dashboard — a SuperLightTUI-based TUI.
//!
//! Public modules so the binary target (main.rs) can access
//! app state, service data, and view rendering.

pub mod app;
pub mod services;
pub mod views;
