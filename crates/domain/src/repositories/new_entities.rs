use crate::entities::{Group, GroupType, Livestock, LivestockType, Tree, TreeType, Building, BuildingType, Variety, VarietyCategory, Breed, Species};

/// DB-Repository-Stub für neue Domain-Entitäten.
/// Vollständige Integration erfordert sqlx-Queries und Backend-Endpunkte.
pub struct NewEntityRepo;

impl NewEntityRepo {
    pub fn new() -> Self { Self }
}
